// Command guest is the file-browser app's client-side WASM module: a full,
// non-terminal nx2 app. It renders a directory listing to the cell-grid surface,
// handles arrow-key navigation locally, and asks its companion (over fproto) to
// change directories — no pkg/te involved. Built as a wasip1 c-shared reactor.
package main

func main() {}
